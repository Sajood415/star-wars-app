export const setCategory = (category) => ({
    type: 'SET_CATEGORY',
    payload: category,
  });
  
  export const setSubItems = (subItems) => ({
    type: 'SET_SUB_ITEMS',
    payload: subItems,
  });
  
  export const setLoading = (loading) => ({
    type: 'SET_LOADING',
    payload: loading,
  });
  